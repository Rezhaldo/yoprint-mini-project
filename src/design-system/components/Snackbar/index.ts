import { ErrorSnackbarContent } from './Error';
import { InfoSnackbarContent } from './Info';
import { SuccessSnackbarContent } from './Success';
import { WarningSnackbarContent } from './Warning';

export const CustomSnackBarContent = {
	success: SuccessSnackbarContent,
	info: InfoSnackbarContent,
	warning: WarningSnackbarContent,
	error: ErrorSnackbarContent,
};
